| <input type="hidden" id="count_thankscard" value="
= @all
| "> <input type="hidden" id="count_send" value="
= @count_tkcard
| "> <input type="hidden" id="count_receiver" value="
= @count_receiver
| "> 
.tab-content.m-3
  #tab-content-0.tab-pane.tabs-animation.fade.show.active[role="tabpanel"]
    .row
      .col-md-6
        .main-card.mb-3.card
          .card-body
            .chartjs-size-monitor
              .chartjs-size-monitor-expand
                div
              .chartjs-size-monitor-shrink
                div
            h5.card-title
              | Detailed statistics
            canvas#myChart[style="width:100%; height:345px"]
        .main-card.mb-3.card
          .card-body
            .chartjs-size-monitor
              .chartjs-size-monitor-expand
                div
              .chartjs-size-monitor-shrink
                div
            h5.card-title
              | Top 1 Sent
            .team-boxed.position-relative[style="height:345px"]
              .container
                .row.people.d-flex.justify-content-center
                  - @user_send.each do |items|
                    .col-md-6.col-lg-6.user-send-item.item
                      .box
                        = avatar_for items
                        h3.name
                          = items.full_name
                        p.description
                          | hãy gửi thư đến 
                          span[style="color:#000; font-style: italic;"]
                            = items.email
                          |  để tạo thankscard với 
                          = items.full_name
                        .social
                          a[href="#"]
                            i.fa-solid.fa-envelope
                          a[href="#"]
                            i.fa-sharp.fa-solid.fa-user
              - if (!@user_send.nil? && @user_send.count > 2)
                button.control.prev[type="button" style="left: 22px;"]
                  span.control-icon
                    i.fa-sharp.fa-solid.fa-chevron-left
                button.control.next[type="button" style="right: 22px"]
                  span.control-icon
                    i.fa-solid.fa-chevron-right
      .col-md-6
        .main-card.mb-3.card
          .card-body
            .chartjs-size-monitor
              .chartjs-size-monitor-expand
                div
              .chartjs-size-monitor-shrink
                div
            h5.card-title
              | Top 1 receiver
            .team-boxed.position-relative[style="height:345px"]
              .container
                .row.people.d-flex.justify-content-center
                  - @user_receiver.each do |items|
                    .col-md-6.col-lg-6.item.user-receiver-item
                      .box
                        = avatar_for items
                        h3.name
                          = items.full_name
                        p.description
                          | hãy gửi thư đến 
                          = items.email
                          |  để tạo thankscard với 
                          = items.full_name
                        .social
                          a[href="#"]
                            i.fa-solid.fa-envelope
                          a[href="#"]
                            i.fa-sharp.fa-solid.fa-user
              - if (!@user_receiver.nil? && @user_receiver.count > 2)
                button.control.prev[type="button" style="left: 22px;"]
                  span.control-icon
                    i.fa-sharp.fa-solid.fa-chevron-left
                button.control.next[type="button" style="right: 22px"]
                  span.control-icon
                    i.fa-solid.fa-chevron-right
= javascript_include_tag "chart.js"
= javascript_include_tag "view_report_user.js"